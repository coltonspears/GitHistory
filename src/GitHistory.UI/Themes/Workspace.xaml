<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:GitHistory.UI.Controls">
    <Style x:Key="WorkspaceSplitter" TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="GridSplitter">
            <Border Background="{TemplateBinding Background}">
                <Border x:Name="Grip" Width="2" HorizontalAlignment="Center" Margin="0,5" Background="{DynamicResource LineBrush}" CornerRadius="1"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="ResizeDirection" Value="Rows">
                    <Setter TargetName="Grip" Property="Width" Value="Auto"/><Setter TargetName="Grip" Property="Height" Value="2"/>
                    <Setter TargetName="Grip" Property="HorizontalAlignment" Value="Stretch"/><Setter TargetName="Grip" Property="VerticalAlignment" Value="Center"/><Setter TargetName="Grip" Property="Margin" Value="5,0"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                <Trigger Property="IsDragging" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4"/></Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style TargetType="{x:Type ui:Sidebar}">
        <Setter Property="Background" Value="{DynamicResource SidebarBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Sidebar}">
                    <Border x:Name="Chrome" Width="{TemplateBinding ExpandedWidth}" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" ClipToBounds="True">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <Grid x:Name="HeaderGrid" Margin="12,4,9,0">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Heading" ContentSource="Header" VerticalAlignment="Center" TextElement.FontSize="11" TextElement.FontWeight="SemiBold" TextElement.Foreground="{DynamicResource MutedBrush}"/>
                                <Button x:Name="Toggle" Grid.Column="1" Width="28" Height="28" MinHeight="28" Padding="0" VerticalAlignment="Center"
                                        Command="{x:Static ui:Sidebar.ToggleCommand}" CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                        Content="‹" FontSize="23" ToolTip="Collapse sidebar" AutomationProperties.Name="Collapse sidebar"
                                        Style="{StaticResource QuietButton}"/>
                            </Grid>
                            <ContentPresenter x:Name="Body" Grid.Row="1" ContentSource="Content" Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <Thumb x:Name="PART_ResizeGrip" Grid.RowSpan="2" Width="6" HorizontalAlignment="Right" Cursor="SizeWE" Focusable="True"
                                   ToolTip="Resize sidebar · Drag or use arrow keys" AutomationProperties.Name="Resize sidebar">
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Border Background="Transparent">
                                            <Border x:Name="Grip" Width="2" Background="Transparent" HorizontalAlignment="Right" Margin="0,8,0,8" CornerRadius="1"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                                            <Trigger Property="IsDragging" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource AccentBrush}"/></Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="Chrome" Property="Width" Value="{Binding CollapsedWidth, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter TargetName="Heading" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="HeaderGrid" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="HeaderGrid" Property="HorizontalAlignment" Value="Center"/>
                            <Setter TargetName="Body" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_ResizeGrip" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="Toggle" Property="Content" Value="›"/>
                            <Setter TargetName="Toggle" Property="ToolTip" Value="Expand sidebar"/>
                            <Setter TargetName="Toggle" Property="AutomationProperties.Name" Value="Expand sidebar"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
